# Per-repo command allowlist. No dev verbs declared yet; present so coily's
# repocfg loader recognizes the repo and the lockdown discipline is uniform
# across coilysiren/* checkouts. Add verbs here as they emerge.

commands: {}

# Catalog metadata for the cross-repo knowledge graph.
# Schema: coilysiren/coilyco-ai#420 (tracker).
catalog:
  kind: Component
  type: configuration
  system: ops
  owner: kai
  lifecycle: production
  description: "Personal dotfiles, shell config, GPG/SSM tooling, and shared utility scripts symlinked into sibling coilysiren/* repos."
  dependsOn: []
  providesApis:
    - shared-scripts
